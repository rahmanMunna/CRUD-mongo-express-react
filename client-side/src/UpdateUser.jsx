import { useLoaderData } from "react-router-dom";

const UpdateUser = () => {
    const id = useLoaderData();
    console.log(id)
    return (
        <div>
            
        </div>
    );
};

export default UpdateUser;